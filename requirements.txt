# server.py uses only the Python standard library — no third-party packages required.
#
# html_to_pdf.py needs Playwright (Chromium-backed) for HTML → PDF conversion.
playwright>=1.40

# System requirement (not installable via pip):
#   - openssl  (used once to generate a self-signed TLS certificate, for server.py)

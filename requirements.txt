# server.py uses only the Python standard library — no third-party packages required.
# This file is intentionally empty (besides this note) for tooling compatibility.
#
# System requirement (not installable via pip):
#   - openssl  (used once to generate a self-signed TLS certificate)
